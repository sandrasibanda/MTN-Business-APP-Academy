<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rejang">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReJang">
        <activity
            android:exported="true"
            android:name=".NavigateActivity"
            android:label="@string/title_activity_navigate"
            android:theme="@style/Theme.ReJang.NoActionBar"></activity>
        <activity android:name=".MainActivity" />
        <activity
            android:exported="true"
            android:name=".SlashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>